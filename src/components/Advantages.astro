---
import type { SiteContent } from "../data/content";

type Props = {
  content: SiteContent;
};

const props = Astro.props;
---

<section class="section" id="advantages">
  <div class="section-heading">
    <p class="eyebrow eyebrow-underline">{props.content.advantages.eyebrow}</p>
    <h2>{props.content.advantages.title}</h2>
    <p>{props.content.advantages.description}</p>
  </div>

  <div class="cards-grid">
    {props.content.advantages.items.map((item) => (
      <article class="info-card info-card-accent">
        <h3>{item.title}</h3>
        <p>{item.description}</p>
      </article>
    ))}
  </div>
</section>
